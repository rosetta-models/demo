model:
  name: DEMO
  defaultSerialisationFormat: RUNE_JSON
generators:
  namespaces:
    - demo.*
    - com.rosetta.model
namespaceConfig:
  - id: demoExampleRecordkeeping
    namespace: demo.example.recordkeeping.*
    readOnly: true
    origin:
      modelImport: xsd
    schemaConfig:
      schema: demoExampleRecordkeeping
      configPath: xml-config/demo.example.recordkeeping-xml-config.json
